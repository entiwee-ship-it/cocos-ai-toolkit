{
  "name": "@cocos-ai/cli",
  "version": "0.9.1",
  "private": true,
  "type": "module",
  "bin": {
    "cocos-ai": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cocos-ai/client": "0.9.1",
    "@cocos-ai/protocol": "0.9.1",
    "zod": "^4.3.6"
  }
}
